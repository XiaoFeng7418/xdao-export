# X岛串导出工具

一个 Windows 桌面小工具，用来把 X 岛（nmbxd1.com）的某一个串完整导出为独立的 HTML 文件。

## 功能

- 登录 X 岛账号后读取超过前 100 页的完整内容（也可手动粘贴 userhash 饼干）。
- 每个发言人的饼干（user_hash）都会显示，PO 有明显的高亮标记。
- 支持一次导出多个串，每个串单独生成一个文件。
- 支持“所有人发言”和“只抓 PO 发言”两种范围。
- 图片（含动图）会嵌入到 HTML 文件里，文件可离线打开、打印。
- 导出目录可配置，文件按串标题命名，无标题时取串的第一句话。

## 运行

需要 Python 3.10+（内置 tkinter）：

```powershell
python main.py
```

## 说明

- 登录信息只保存在本机配置目录，不保存明文密码。
- 抓取会逐页请求，串很长时请耐心等待。
