# X岛串导出工具

一个 Windows 桌面小工具，用来把 X 岛（nmbxd1.com）的某一个串完整导出为本地文件。

## 功能

- 登录 X 岛账号后读取超过前 100 页的完整内容（也可手动粘贴 userhash 饼干）。
- 每个发言人的饼干（user_hash）都会显示，PO 有明显的高亮标记。
- 支持一次导出多个串，每个串单独生成一个文件。
- 支持“所有人发言”和“只抓 PO 发言”两种范围。
- 支持两种导出格式：
  - HTML：图片全部嵌入，可离线打开、打印、另存为 PDF；
  - TXT：纯文本，图片以链接形式保留。
- 标题和作者（name）在非默认值时会显示。
- 导出目录可配置，文件按串标题命名，无标题时取串的第一句话。

## 下载（打包版）

无需安装 Python，直接双击即可运行的 Windows 程序，请到本仓库的 Releases 页面下载 `X岛串导出工具.exe`：

https://github.com/XiaoFeng7418/xdao-export/releases

## 从源码运行

需要 Python 3.10+（内置 tkinter）：

```powershell
python main.py
```

## 说明

- 登录信息只保存在本机配置目录，不保存明文密码。
- 抓取会逐页请求，串很长时请耐心等待。
